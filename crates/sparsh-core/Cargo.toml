[package]
name = "sparsh-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Session, config and structured-command engine behind the Sparsh shell"
repository = "https://github.com/oraclevs/sparsh"

[dependencies]
indexmap = "2"
rayon = "1"
spar = { path = "../../../spar", version = "0.6.0" }
spar-command = { path = "../../../spar-command", version = "0.1.0" }
spar-process = { path = "../../../spar-process", version = "0.1.0" }

[dev-dependencies]
tempfile = "3"
