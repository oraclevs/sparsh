export function greet(prefix: str, name: str) -> str {
    return "${prefix}, ${name}";
};

export function build(profile: str) -> shell {
    return shell {
        cargo build --profile "${profile}";
    };
};

export function showFile(file: str) -> shell {
    return shell {
        cat "${file}";
    };
};
